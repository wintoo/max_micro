#!/usr/bin/env groovy

//configure using Jenkins-Shared-Libraries and using "part2" branch
@Library("Jenkins-Shared-Libraries@part2") _

// Entry point into Jenkins-Shared-Libraries
jenkinsJob.call()
